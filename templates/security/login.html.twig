{% extends'base.html.twig' %}
{% block body %}
    <form action="{{ path('security_login') }}" method="post">
        <div class="form-group">
            <input placeholder="email adress" required name="_username" type="text" class="form-control">
        </div>
        <div class="form-group">
            <input placeholder="mot de pass" required name="_password" type="password" class="form-control">
        </div>


        <div class="form-group">


            <button type="submit"  class="btn btn-primary btn-block" href="{{path('yahala')}}">connect</button>
        </div>


        <div class="col-md-6">
            <div class="ml-md-3 mt-3 mt-md-0 forgot-pass">
                <a href="#">Forgot Password?</a>

                <p class="mt-1">Don't have account? <a href="{{path('userfront')}}">Sign Up here as a Company</a></p>
                <p class="mt-1">Don't have account? <a href="{{path('new1')}}">Sign Up here as a Job seeker</a></p>
            </div>
        </div>
        </div>
    </form>
{% endblock %}
