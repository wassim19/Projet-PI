{#
{% extends 'base.html.twig' %}

{% block title %}Modifier Reclamation{% endblock %}

{% block body %}
    <h1>Modifier Reclamation</h1>

    {{ include('reclamation/_form.html.twig', {'button_label': 'Update'}) }}
     {{ include('reclamation/_delete_form.html.twig') }}
    <div > <a  href="{{ path('reclamation_delete',{'id': reclamation.id}) }}">
            <button type="submit" class="btn btn-primary btn-md" >
                supprimer
            </button></a>
    </div>
    <p> </p>

    <a href="{{ path('reclamation_index') }}">back to list</a>


{% endblock %}
#}
{% extends 'base.html.twig' %}
{% block body %}

    <!--=================================
    inner banner -->
    <div class="header-inner bg-light text-center">
        <div class="container">
            <div class="row">
                <div class="col-12">
                    <h2 class="text-primary">update a complaint</h2>
                    <ol class="breadcrumb mb-0 p-0">
                        <li class="breadcrumb-item"><a href="{{ path('reclamation_index') }}">complaints</a></li>
                        <li class="breadcrumb-item active"> <i class="fas fa-chevron-right"></i> <span> update a complaint </span></li>
                    </ol>
                </div>
            </div>
        </div>
    </div>
    <!--=================================
    inner banner -->

    <div class="tab-content" id="myTabContent">
        <div class="tab-pane fade active show" id="Job-detail" role="tabpanel" aria-labelledby="Job-detail-tab">
            <section class="space-ptb">
                <div class="container">
                    {{ form_start(form) }}
                    {{ form_row(form.motif,{ 'attr':{ 'placeholder':"motif",'class':"form-control"}}) }}
                    {{ form_row(form.message,{ 'attr':{ 'placeholder':"message",'class':"form-control"}}) }}

                    {{ form_row(form.gsm,{ 'attr':{ 'placeholder':"gsm",'class':"form-control"}}) }}
                    {{ form_row(form.company,{ 'attr':{ 'class':"form-control"}}) }}

                    {{ form_row(form.accept) }}
                    <div align="center"><button type="submit" class="btn btn-primary btn-md">update</button></div>
                    {{ form_end(form) }}
                </div>
            </section>
        </div>
    </div>
{% endblock %}
