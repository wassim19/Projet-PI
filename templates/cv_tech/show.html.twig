{% extends 'base.html.twig' %}

{% block title %}CvTech{% endblock %}

{% block body %}
    <h1>CvTech</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cv_tech.id }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ cv_tech.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cv_tech_index') }}">back to list</a>

    <a href="{{ path('cv_tech_edit', {'id': cv_tech.id}) }}">edit</a>

    {{ include('cv_tech/_delete_form.html.twig') }}
{% endblock %}
