{% extends 'base.html.twig' %}



{% block body %}
    <h1>Surfer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ surfer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ surfer.name }}</td>
            </tr>
            <tr>
                <th>First_name</th>
                <td>{{ surfer.firstName }}</td>
            </tr>
            <tr>
                <th>Cin</th>
                <td>{{ surfer.cin }}</td>
            </tr>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('surferindex') }}">back to list</a>

    <a href="{{ path('surferedit', {'id': surfer.id}) }}">edit</a>

    {{ include('surfer/_delete_form.html.twig') }}
{% endblock %}
